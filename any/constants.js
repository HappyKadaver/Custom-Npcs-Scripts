/**
 * There is no way to tell Custom NPCs to load the files in a specific order.
 * For that reason every constant will be defined as a function.
 * 
 */
 
/**
 * Constants for roam.js
 * 
 */
 
 /*
  * Key for storing a location as tempData
  * 
  */
function CURRENT_ROAM_POINT_KEY() {
    return "currentLocation"
}

function DEFAULT_STAY_DURATION() {
    return 500
}
